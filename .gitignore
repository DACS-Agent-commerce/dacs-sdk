node_modules/
dist/
build/
*.log
.env
.env.*
!.env.example
.DS_Store
coverage/
.turbo/
*.tsbuildinfo

# Conformance vectors are synced from DACS-Standard at a pinned ref
# (npm run conformance:sync); not vendored into git.
vendor/

# Ephemeral npm-audit JSON written by the CI dependency-audit step.
audit.json
